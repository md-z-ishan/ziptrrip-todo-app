{
  "name": "ziptrip-todo-app",
  "version": "1.0.0",
  "description": "Premium Todo App for Ziptrip Internship",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start"
  },
  "keywords": ["todo", "react", "express", "ziptrip"],
  "author": "Zishan",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
